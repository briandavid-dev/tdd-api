const users = require("./");

describe("Endpoints", () => {
  describe("Users", () => {
    describe("Get", () => {
      it("Return a user json", () => {
        const axios = jest.fn();
        expect(1).toEqual(1);
      });
    });
  });
});
